target("sphere")
    set_kind("binary")
    add_files("sphere.c")

target("planeclip")
    set_kind("binary")
    add_files("planeclip.c")

target("paraboloid")
    set_kind("binary")
    add_files("paraboloid.c")

target("saddle")
    set_kind("binary")
    add_files("saddle.c")

target("drawsphere")
    set_kind("binary")
    add_files("drawsphere.cpp")

target("mesh")
    set_kind("binary")
    add_files("mesh.c")

target("surface")
    set_kind("binary")
    add_files("surface.c")
    